// Minimal root build.gradle to provide common properties for subprojects
ext.projectVersion = '1.0.0'
ext.gdxVersion = '1.13.1'
ext.enableGraalNative = 'false'

allprojects {
    repositories {
        mavenCentral()
        mavenLocal()
    }
}

// No-op for root project; the actual Java project is in triangle/lib

